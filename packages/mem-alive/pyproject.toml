[project]
name = "mem-alive"
version = "0.1.1"
description = "A memory layer for AI agents"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "indira-kumar", email = "indirakumarak@gmail.com" },
]
dependencies = [
    "httpx>=0.28.1",
    "numpy>=2.4.6",
]

[project.optional-dependencies]
lancedb = ["lancedb"]

[project.urls]
Homepage = "https://github.com/Indira-kumar/mem-alive"
Repository = "https://github.com/Indira-kumar/mem-alive"
Issues = "https://github.com/Indira-kumar/mem-alive/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mem_alive"]
